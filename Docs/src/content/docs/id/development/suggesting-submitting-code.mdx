---
title: Menyarankan/Mengirim Kode
description: Dokumentasi seputar cara untuk menyarankan atau mengirimkan kode ke repositori Corsace.
lastUpdated: 2023-09-18
sidebar:
  order: 3
---

## Bacaan Prasyarat

*   [Pengembangan/Memulai](../../..)

## Memastikan Fork Telah Tersinkronisasi

:::caution
For anything outside of documentation work, it's highly suggested that you configure remote branching to keep your fork up to date.\
You can read more about it [here](https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/configuring-a-remote-repository-for-a-fork)

Di samping itu, kamu juga dapat menyinkronkan fork kamu dengan mudah melalui berbagai metode yang tertera pada [halaman ini](https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork)\
Dokumentasi berikut disediakan dengan anggapan bahwa kamu telah mengatur fork-mu agar terhubung dengan repositori Corsace.
:::

Before submitting a pull request, make sure that your fork is fully synced with the latest commit in the Corsace repository in order to avoid merge conflicts.\
If you already committed changes A B C D E F G H I J to the code in your fork before updating, you can use the following git options:

```bash
git fetch [CORSACE REPOSITORY]
git rebase [CORSACE REPOSITORY]/master

git cherry-pick A B C F # To commit only specific commits you created since
# OR
git cherry-pick A^..J # Commit all of the commands you created since

git push -f 
#OR 
git push --force
```

`[CORSACE REPOSITORY]` is the name of the remote you created. You would most likely have called it `upstream` if you use the example GitHub provides.

This should now have you up to date to the latest commit on the Corsace repository alongside the commits you wanted.

## Submitting New/Changed Documents

Make sure to run `npm run lint` (as well as `npm run lintfix` for anything automatically fixable), and fix the issues that appear if any.
Create a Pull Request whenveer ready. Currently, there is no specifications needed for PR/commit naming.\
Afterwards, ping VINXIS (`corsace.io` or `vinxis` on discord) or ThePooN (`thepoon` on discord) in the #dev channel in the [Corsace Discord Server](https://discord.gg/Z6vEMsr), and it should typically be checked within a day.
